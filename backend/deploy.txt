// Swagger docs
http://localhost:8080/docs

// Docker build process
docker-compose down
docker-compose build --no-cache
docker-compose up -d


// Passwords 
admin@example.com
admin

user@example.com
user

Execute the migration.sql on first database startup!!
